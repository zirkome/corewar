PARSING DE LA COMMANDE D'ENTREE

##################################################################

NOTE :

-Parse la commande peut importe l'ordre.
-Prend en parametre un tableau de structure de 4 cases seulement
vu que vous ne pouvez traiter que 4 champions en meme temps.
-Normalement il ne vous reste qu'a remplir les cases par defauts,
celles qui n'ont pas etaient parametree (ex : si le num du prog
n'est pas mis)

QUESTION :

-Pour le dump je l'ai traite comme un int mais j'ai laisse un
get_nbr_hexa si c'est un hexa qui est en parametre
-Pour le load_address j'ai pris un hexa comme parametre, s'il
faut un int, remplacer par un get_nbr au lieu de get_nbr_hexa.


Dites moi s'il y a le moindre souci.
